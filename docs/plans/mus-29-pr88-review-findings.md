# PR 88 review — raw finder output (UNVERIFIED)

Context: code-review at high effort over PR 88 (feat/agent-loop → master, the MUS-29
integration PR, head 554dccf). Ten finder angles fanned out; **eight completed, two were
killed mid-run** on Musa's "stop for now" (Angle B removed-behavior audit, Angle C
cross-file tracer — no findings emitted). **No verification pass has run: every finding
below is a candidate, not a confirmed defect.** Expect overlaps between angles.

Status at stop: PR 88 open, full CI green (ci-ok, 4 test legs, frontend, lint, mypy,
rules eval), merge on hold.

## Angle: conventions (CLAUDE.md compliance) — 0 findings

Clean. All new tests use setUpTestData/fixtures, all LLM calls mocked, no pre-existing
test modified, bundle rebuilt and committed, integration-branch size sanctioned by
workflow rule 4.

## Angle: simplification — 8 findings

```json
[
  {"file": "project/app/models.py", "line": 510, "summary": "AgentLeadRun.steps_used and tool_calls_used are denormalized counters duplicating what the append-only AgentStep log already encodes (count of kind='llm_call' / kind='tool_result' rows).", "failure_scenario": "The loop itself never trusts these columns — it recomputes both counts from the steps at resume (loop.py:127-128) — so the columns can silently drift from the log after a lost-claim race yet are served as truth by the trace API; the only reader (views_trace.py:47-48) already has the steps in hand. Simpler: drop the two columns, derive the counts from steps in the trace view, and delete the steps_used/tool_calls_used parameters threaded through Checkpoint.append's five call sites and AgentOutcome (whose two counter fields no production caller reads — outreach.py uses only .error and .draft_text)."},
  {"file": "project/app/models.py", "line": 498, "summary": "AgentLeadRun.STATUS_GATHERING is defined and included in NON_TERMINAL_STATUSES but never assigned anywhere in the codebase — the loop only ever writes claimed, drafting, done, failed, exhausted.", "failure_scenario": "A dead status widens the claim CAS filter and the resume-scan index semantics for a state that cannot occur; simpler: delete the constant and its NON_TERMINAL_STATUSES entry (grep shows its only occurrences are the two definition lines)."},
  {"file": "project/app/services/llm/openai_compatible.py", "line": 207, "summary": "agenerate_chat is a byte-for-byte copy of agenerate except for one line (the request-builder call) — ~15 duplicated lines of client/timing/raise_for_status/error-mapping — and claude.py:225 repeats the same pattern against its own agenerate.", "failure_scenario": "The next fix to the error/latency handling must be applied in two places per adapter or the chat and completion paths diverge; simpler: extract the shared body into one private helper per adapter and make both public methods one-line delegations differing only in which request builder they call."},
  {"file": "project/app/services/agent/state.py", "line": 62, "summary": "Step hashes are smuggled through StepRecord.payload under sentinel keys (PAYLOAD_REQUEST_SHA256/PAYLOAD_RESULT_SHA256) that _append_sync must pop back out (lines 279-280) so the stored JSON stays clean.", "failure_scenario": "The payload is temporarily not the documented shape (a forgotten pop would persist hashes into the trace JSON); simpler: add defaulted request_sha256/result_sha256 fields to the frozen StepRecord dataclass, have loop.py set them directly and _append_sync read them — same columns written, no sentinel constants, payload correct by construction."},
  {"file": "project/app/services/agent/tools.py", "line": 234, "summary": "All four tool schemas declare zero properties, so the properties/allowed argument-filtering in execute_tool always produces {} and every _render_* function takes an `arguments` parameter that none of them reads.", "failure_scenario": "Ten lines of plumbing exercise a code path that is a no-op by construction; simpler: renderers take `context` only and execute_tool ignores `arguments` after the UnknownTool check — behavior identical since server-side binding, not filtering, is what makes a model-supplied lead_id inert."},
  {"file": "frontend/src/util/trace.ts", "line": 83, "summary": "hidesTraceToggle duck-types the 404 ('status' in error, cast, compare) with a comment justifying staying import-free, but the sibling test file already imports ../src/api/client.ts under node:test, proving the ApiError import is safe.", "failure_scenario": "The structural check matches any Error someone hangs a status:404 on; simpler and stricter: import ApiError and use instanceof plus status === 404 — client.ts has no JSX and no import-time side effects."},
  {"file": "project/app/services/agent/state.py", "line": 53, "summary": "STATUS_DRAFTING/DONE/FAILED/EXHAUSTED are hand-copied mirrors of AgentLeadRun.STATUS_* strings, with the docstring conceding that drift between the two copies is only caught by test asserts.", "failure_scenario": "A renamed or added status must be edited in two modules that already talk to each other, or a checkpoint writes a status the model's NON_TERMINAL_STATUSES logic doesn't recognize; simpler: keep one definition — models.py can assign its class constants from these state.py strings (no cycle), leaving loop.py's Django-free import path untouched."},
  {"file": "project/app/tests_agent_loop_loop.py", "line": 27, "summary": "_tool_result, _final, and the scripted fake chat client are copy-pasted between tests_agent_loop_loop.py (27-63) and tests_agent_loop_assembly.py (47-108, slight variation), plus a near-identical lead-seeding fixture builder repeated across the agent test modules.", "failure_scenario": "The 'component modules do not import each other' rationale was a parallel-development constraint that ends with this merged PR; any change to LLMResult construction needs the same edit in two-plus files that can drift; simpler: move the builders and one parameterizable fake client into a shared tests_agent_loop_support.py, mirroring tests_telemetry_support.py."}
]
```

## Angle: efficiency — 6 findings

```json
[
  {"file": "project/app/services/agent/state.py", "line": 158, "summary": "create_lead_runs issues a per-lead transaction + get_or_create (SELECT then INSERT) in a Python loop, ~2 queries per lead on every agent-enabled run and every resume.", "failure_scenario": "For N leads this is ~2N round-trips (plus N savepoints) before the loop even starts; replace with one bulk_create(ignore_conflicts=True) followed by a single filter().values_list() to build the lead_id->pk map in 2 queries total."},
  {"file": "project/app/services/outreach.py", "line": 2370, "summary": "agent_plans calls load_prior_steps(run_pks[...]) once per work item, one AgentStep query per lead — an N+1 that runs (and returns empty) on every fresh agent-enabled plan run.", "failure_scenario": "N leads = N extra queries every planner run, contradicting the adjacent 'Two bulk reads rather than two per lead' comment; do one filter(lead_run_id__in=...) ordered query and group rows in Python — 1 query regardless of lead count."},
  {"file": "project/app/services/outreach.py", "line": 2340, "summary": "similar_won_deals_for(item.lead, leads) is called once per work item and each call re-walks every other lead's prefetched events, rebuilds its deal list, and re-runs sanitize_untrusted on the same deal metadata — O(work x leads) redundant CPU.", "failure_scenario": "With W work items and L leads that is WxL event scans and repeated sanitization of identical strings; precompute one per-lead summary pass before the comprehension, then score each pair against the cached summaries."},
  {"file": "project/app/services/outreach.py", "line": 2349, "summary": "The prior_actions prefetch loads every OutreachAction ever recorded for the run's leads with no limit, and the set grows by one row per lead per planner run, all of it dict-built and sanitized into every ToolContext.", "failure_scenario": "After K runs this query returns O(K x leads) rows and the whole history is serialized into each lead's tool snapshot even though execute_tool truncates the rendered result at 2000 chars; cap the fetch (e.g. latest ~10 actions per lead)."},
  {"file": "project/app/services/outreach.py", "line": 2367, "summary": "agent_plans eagerly builds the full ToolContext (event sort, per-field sanitization, similar-deals snapshot) for every work item, including resumed leads whose step log already ends in a final — run_agent_lead short-circuits those at loop.py:135 without ever reading the context.", "failure_scenario": "On a resume where most leads finished, all the phase-2 snapshot work is thrown away for every completed lead; load prior_steps first and skip context building when the last step is final."},
  {"file": "project/app/services/agent/loop.py", "line": 167, "summary": "Each loop iteration rebuilds the entire conversation from scratch — fold_messages re-wraps every persisted tool result in untrusted fences and _rendered_request re-serializes the whole message list to canonical JSON for hashing — O(steps^2) rework across a run.", "failure_scenario": "A 6-step run redoes ~sum(1..6) message constructions and full-history hash passes instead of ~6; keep a running messages list across iterations so each iteration folds only its delta — minor next to the provider call, but pure rework."}
]
```

## Angle: reuse — 6 findings

```json
[
  {"file": "project/app/services/llm/openai_compatible.py", "line": 207, "summary": "agenerate_chat duplicates the entire HTTP call body of the existing agenerate, and _chat_request rebuilds _request's URL/auth-header envelope.", "failure_scenario": "The post/raise_for_status/error-mapping/latency sequence and the auth-header + chat-completions URL assembly are copied verbatim from agenerate/_request in the same file; a future fix on one path silently misses the other — extract a shared async post helper used by both."},
  {"file": "project/app/services/llm/claude.py", "line": 225, "summary": "agenerate_chat duplicates the credential-check/timing/error-mapping body of the existing agenerate, and _chat_request_kwargs re-assembles _request_kwargs' model/max_tokens/timeout envelope.", "failure_scenario": "The scaffolding is copied from agenerate including the subtle only-pass-timeout-when-set rule; a change to credential handling or the timeout rule in one method drifts from the other. Share one call helper and one kwargs-envelope builder."},
  {"file": "project/app/services/dispatch.py", "line": 44, "summary": "The approval digest is computed with inline hashlib in two new places that must agree byte-for-byte, instead of one shared helper such as the existing genai.sha256_of.", "failure_scenario": "dispatch() and QueueApproveView (views_queue.py:431) each inline the sha256-hexdigest over effective_copy; the whole send gate depends on these two staying identical, and the codebase already has the canonical hasher (sha256_of in services/telemetry/genai.py:467). Route both through one approval-digest helper."},
  {"file": "project/app/services/agent/tools.py", "line": 181, "summary": "build_tool_context and similar_won_deals_for hardcode lead.events.all() and re-sort/re-format events, re-implementing outreach's _events_list seam and _format_events_for_prompt's timestamp rendering.", "failure_scenario": "Bypasses the deliberately duck-typed seam that lets the rules eval and tests_logic feed plain-list leads without a database; any harness that passes list-backed leads breaks only on the agent path, and a prefetch-bypassing call reintroduces the N+1 _events_list exists to prevent."},
  {"file": "project/app/services/agent/product_catalog.py", "line": 13, "summary": "PRODUCT_CATALOG restates verbatim the product facts already hardcoded in outreach's copy prompt, creating a second source of product truth.", "failure_scenario": "The facts duplicate the opening sentence of _build_copy_prompt (outreach.py:1171); if either side is edited alone, the get_product_details tool feeds the model claims the prompt (and the verifier's grounding) no longer matches. Hoist one shared constant and build the prompt's product sentence from it."},
  {"file": "frontend/src/util/trace.ts", "line": 83, "summary": "hidesTraceToggle structurally sniffs a 'status' property on Error, re-implementing the typed ApiError the API client already exports.", "failure_scenario": "Duplicates ApiError in frontend/src/api/client.ts; any other error object that grows a status field silently changes toggle-hiding behavior; use instanceof ApiError with status 404 (or match the no-agent-trace code) instead."}
]
```

## Angle: altitude — 8 findings

```json
[
  {"file": "project/app/services/outreach.py", "line": 1753, "summary": "The agent path funnels its draft into the shared phase-4 gates, but services/verify.py was never extended to ground against the new tool-fact surface (other leads' won-deal premiums, AE calendar names/dates, product catalog) the agent deliberately feeds the model — verify_copy still grounds only against the Lead record and its events.", "failure_scenario": "Any draft that actually uses the gathered context ('an agency like yours locked $50,000 in premium', 'Avery Collins has a slot June 22') trips _check_amounts/_check_iso_dates/_check_contact_name and fails closed to needs_human, so the feature's entire added value routes to the review queue as false violations (or pressures someone to weaken the verifier). Deeper fix: extend verify_copy's grounding corpus to accept the per-lead ToolContext snapshot (already frozen, deterministic, persisted)."},
  {"file": "project/app/services/outreach.py", "line": 1750, "summary": "Budget-exhausted agent runs are signaled by returning an empty draft and letting phase 4's shape gate (validate_copy) reject it — repurposing the injection-steering output gate as the exhaustion channel instead of using the existing CopyOutcome.error -> _describe_failure mechanism.", "failure_scenario": "The reviewer sees a needs_human row whose further_action describes email shape problems when the real event was 'agent ran out of steps before drafting' — mislabeled-failure noise; have the loop return a budget-exhausted error and let _describe_failure render it honestly."},
  {"file": "project/app/services/outreach.py", "line": 1870, "summary": "Shared phase-3 orchestration (_agenerate_all) forks crash semantics on the mode flag — a raw exception in a gather slot is a needs_human row when the flag is off but a process-level crash when it is on.", "failure_scenario": "A plain bug in shared phase-3 code becomes a 500 for agent-enabled deployments and a quietly-absorbed provider-trouble row for everyone else; make recoverability a property of the error, not the mode."},
  {"file": "project/app/services/outreach.py", "line": 2536, "summary": "Phase-5 finalize gains agent-only special cases layered onto the shared row-writing path — the flag-gated pre-read that dedupes against trace_run_id, plus the AgentClaimLost filter at line 2475 — instead of making finalize idempotent as one unconditional mechanism.", "failure_scenario": "Single-shot re-entrancy keeps relying on a different mechanism (fresh UUID per run) and any future re-entrant single-shot caller silently duplicates rows; make finalize idempotent per (trace_run_id, lead) unconditionally and adjust the perf pin once."},
  {"file": "project/app/services/agent/state.py", "line": 53, "summary": "STATUS_* literals mirror AgentLeadRun.STATUS_* — a parallel definition of the same enum held in sync only by test assertions, justified by a must-not-import-models rule the module already breaks with lazy model imports.", "failure_scenario": "A renamed or added status drifts; resume scans then silently exclude runs the loop wrote with the new literal, orphaning them from resume. Deeper fix: a small import-anywhere constants module (chat_types.py precedent) both models.py and state.py import."},
  {"file": "project/app/services/agent/state.py", "line": 235, "summary": "Checkpoint writes work by swapping the owning thread's DatabaseWrapper into the executor thread's slot via the private connections._connections attribute plus inc_thread_sharing — production connection plumbing bent around TestCase fixture visibility, kept safe only by the cross-module convention that phase 3 is ORM-free.", "failure_scenario": "Any future thread-sensitive sync work introduced in phase 3 lands on the same global executor thread and either uses the borrowed connection mid-transaction or races the slot swap; an asgiref/Django upgrade can rename the private attr at will. Deeper fix: ThreadSensitiveContext (dedicated executor per context) or run phase 3 under async_to_sync."},
  {"file": "project/app/views.py", "line": 47, "summary": "The unknown-resume-id guard lives only in OutreachRunView; plan_outreach(resume_run_id=...) itself silently mints a fresh run when the id matches nothing — the validation sits one layer above the mechanism it protects.", "failure_scenario": "A typo'd or stale run id from a management command, shell session, or scheduled job starts a silently fresh run and re-bills every provider call the resume existed to save; validate inside plan_outreach (typed UnknownRun error), view translates to 400."},
  {"file": "project/app/views_queue.py", "line": 427, "summary": "The status-transition <-> live-send-decision invariant is maintained by inline discipline across three view bodies (approve creates, dismiss creates, undo voids) with no model- or service-level mechanism tying a transition to its decision bookkeeping.", "failure_scenario": "The next path that flips these statuses (admin action, bulk job, future API) silently skips decision recording or voiding: an approved action with no decision row makes dispatch() permanently blocked with no visible cause. Deeper fix: service functions beside dispatch() (record_approval/record_rejection/void_send_decisions, one shared body-hash helper) with the views as thin callers."}
]
```

## Angle D: language pitfalls — 6 findings

```json
[
  {"file": "project/app/services/dispatch.py", "line": 48, "summary": "dispatch() verifies the approve_send decision's liveness and body hash before, not inside, the atomic status CAS, so the approval chain it enforces can be swapped out between check and commit.", "failure_scenario": "Worker A reads live decision D1 and passes the hash check; a reviewer undoes (voids D1, status->pending), edits, re-approves (new D2, new sha256, status->approved); A's CAS on status=approved now succeeds and it creates OutboundSend bound to the voided D1 with the stale digest — a send recorded against a voided decision. The liveness/hash re-check must happen inside the atomic block after the CAS."},
  {"file": "project/app/services/agent/state.py", "line": 257, "summary": "_claim_sync only claims runs in NON_TERMINAL_STATUSES, so a run checkpointed failed/exhausted whose OutreachAction row was never finalized is misreported as AgentClaimLost on resume and the lead silently vanishes forever.", "failure_scenario": "Run hits an LLMError; loop checkpoints status failed (committed); process killed before finalize writes rows. On resume: steps don't end in final, claim() returns False (terminal status), loop returns AgentClaimLost, the finalize filter drops the lead from rows. No OutreachAction row, no error surfaced, on this and every subsequent resume — the lead is unrecoverable."},
  {"file": "project/app/views_queue.py", "line": 425, "summary": "QueueApproveView's read-then-write transition check has no row lock or CAS, so two concurrent approves both pass can_transition_to and the second ReviewDecision.objects.create violates the new rd_one_live_send_per_action partial constraint, surfacing as an unhandled IntegrityError 500.", "failure_scenario": "Double-click Approve on a pending action: both requests pass the check, both save status=approved, both create an approve_send decision with voided_at null; the second insert hits the partial unique constraint; nothing catches IntegrityError here (unlike the review-decision endpoint's 409 path) so the client gets a 500. QueueDismissView (531) has the identical shape."},
  {"file": "project/app/views.py", "line": 51, "summary": "OutreachRunView accepts resume_run_id without checking that the agent path is enabled, so with OUTREACH_AGENT_ENABLED off the resume silently runs a fresh single-shot pass under the old run id — re-billing every lead — and crashes with an IntegrityError if the crashed run had committed any rows.", "failure_scenario": "Agent run crashes; on restart the flag is off (or one worker differs). The resume request passes the exists() gate but agent_plans is None: every checkpointed step ignored, every lead re-billed single-shot. If the crashed run had finalized rows, the flag-gated already-finalized filter is skipped and bulk_create violates oa_one_row_per_lead_per_run -> rollback -> 500 after paying for all the provider calls."},
  {"file": "project/app/services/agent/loop.py", "line": 163, "summary": "The tool-call budget is only consulted when computing force_final before the next provider call, while every tool call in the current response is executed unconditionally, so tool_calls_used can overshoot agent_max_tool_calls by nearly a full response.", "failure_scenario": "Cap 8, resumed lead at 7 used: force_final false, model responds with 6 tool_use blocks, all 6 execute — 13 against a documented budget of 8. A model that batches parallel calls makes the cap advisory; the check needs to apply per call inside the execution loop."},
  {"file": "project/app/services/agent/loop.py", "line": 276, "summary": "asyncio.timeout can cancel the task while a checkpoint.append(status=DONE) is mid-flight in the executor thread; the write still commits, and the except handler then appends status=FAILED with no status guard, marking a run that has a persisted final draft as failed.", "failure_scenario": "The per-lead budget expires while the final append's executor work runs: the transaction commits final + done, the awaiting task gets cancelled, TimeoutError branch appends status failed whose UPDATE filters only on pk+claimed_by (no terminality guard), overwriting done->failed. The run carries a valid persisted final draft plus status failed; the lead is reported as provider trouble instead of getting its drafted copy."}
]
```

## Angle A: line-by-line scan — 8 findings

```json
[
  {"file": "project/app/services/agent/loop.py", "line": 138, "summary": "Resume permanently drops leads whose runs reached a terminal failed/exhausted status before a crash: _claim_sync refuses terminal statuses, run_agent_lead maps the refusal to AgentClaimLost, and plan_outreach filters AgentClaimLost out of the rows to write — only KIND_FINAL runs are replayed, so failed/exhausted runs are neither replayed nor re-claimed.", "failure_scenario": "Run with 3 leads: lead A hits LLMError -> run checkpointed failed (terminal); lead B crashes hard -> re-raise, zero rows written. Resume: lead A's claim returns False, AgentClaimLost filtered out — resume completes but lead A never gets an OutreachAction row on this or any later resume; the lead silently vanishes. Same for exhausted-before-crash."},
  {"file": "project/app/views_queue.py", "line": 606, "summary": "QueueUndoView writes status=pending with a plain save() (no CAS/status re-check inside its transaction), so a concurrent dispatch() that flips approved->sent between undo's can_transition_to check (573, stale in-memory status) and undo's save resurrects a SENT action to pending — violating sent-is-terminal — and voids the approve_send decision an OutboundSend already consumed.", "failure_scenario": "Action approved; reviewer clicks Undo while an operator runs dispatch concurrently: dispatch CAS commits approved->sent + OutboundSend row; undo's transaction then sets status=pending. A sent email shows as pending; re-approve then dispatch again -> OneToOne IntegrityError (500) instead of DispatchBlocked."},
  {"file": "project/app/services/agent/loop.py", "line": 200, "summary": "The tool-execution loop runs every tool call in a provider response with no per-call budget check — agent_max_tool_calls is only consulted at the top of the next iteration via force_final — so a single response can exceed the tool budget by an unbounded amount.", "failure_scenario": "Cap 8; a malicious or degenerate response carries 50 tool_use blocks -> all 50 execute, 50 tool_result steps and spans persisted in one append, tool_calls_used=50; the documented budget is bypassed within one response."},
  {"file": "project/app/views.py", "line": 51, "summary": "A resume request is honored only when runtime.agent_enabled is True: with the flag off, plan_outreach(resume_run_id=...) validates the id, reuses it on the run span, but silently runs a fresh single-shot pass — re-billing every lead — and skips the agent-only already-finalized filter, so it can violate oa_one_row_per_lead_per_run.", "failure_scenario": "Operator sets the flag off to stop the bleeding and posts the resume id: 200-path re-generates every lead single-shot under the old trace_run_id, discarding all checkpointed work with no warning; if the crashed run had finalized any rows, bulk_create inserts a second (trace_run_id, lead) row -> IntegrityError -> 500."},
  {"file": "project/app/management/commands/ingest_data.py", "line": 23, "summary": "Synthetic AE slots are pinned to the fixed anchor week 2026-06-22..26, but build_tool_context filters slots with wall-clock today (outreach.py:2291) — so on any run after 2026-06-26 every seeded slot is in the past and check_ae_calendar always returns an empty list.", "failure_scenario": "Today (2026-08-13) an operator enables the agent against seeded demo data: _slot_is_upcoming rejects all 10 slots, ae_slots is empty in every ToolContext, and the calendar tool permanently renders an empty list — defeating the tool the seed exists to back."},
  {"file": "project/app/views_queue.py", "line": 535, "summary": "QueueDismissView creates the reject_send ReviewDecision with reviewer=editor_of(request) unguarded, while QueueApproveView 401s on an empty reviewer — a session user with no email records an anonymous send-rejection audit row.", "failure_scenario": "An authenticated user whose email is blank dismisses a queue item: a reject_send decision with an empty reviewer is committed — recorded evidence with no who — while the same session is blocked from approving with 401; inconsistent audit guarantee between the two send kinds."},
  {"file": "frontend/src/pages/ReportsPage.tsx", "line": 49, "summary": "TraceSection's load() only fires when phase === 'idle', so once a fetch fails the error state is sticky: collapsing and re-expanding never retries the request.", "failure_scenario": "Transient network blip on first expand -> phase error; the user closes and reopens the toggle, the guard returns immediately -> the error persists until a full page reload even though the endpoint is healthy again."},
  {"file": "project/app/services/agent/loop.py", "line": 198, "summary": "A forced-final response's invented tool_calls are discarded for execution but still persisted verbatim in the llm_call step payload, leaving a step log where an llm_call requests tools that have no matching tool_result step.", "failure_scenario": "Force-final call (no tools offered) returns text plus a hallucinated tool_use block: the persisted payload lists the call, so the reports page renders a model-call step implying a tool request that never ran, and any future fold of that log produces an assistant tool_use message with no tool_result — which the Anthropic API rejects with a 400."}
]
```

## Angle E: wrapper correctness — 7 findings

```json
[
  {"file": "project/app/services/llm/openai_compatible.py", "line": 257, "summary": "A tool call whose function.arguments is an empty string is treated as a provider contract violation (json.loads on empty raises) and fails the whole call non-retryably, even though several OpenAI-compatible providers emit an empty string instead of empty-object JSON for zero-argument tools — and all four agent tools are zero-argument.", "failure_scenario": "Agent enabled with a groq/deepseek-style provider; model calls get_lead_history and the provider returns empty-string arguments -> JSONDecodeError -> LLMMalformedResponseError (non-retryable) -> run checkpointed failed and the lead lands in needs_human with a contract-violation message; every lead whose model uses a tool fails the same way."},
  {"file": "project/app/services/llm/openai_compatible.py", "line": 254, "summary": "Tool-call entries with a missing id/name or with parsed arguments that are valid JSON but not an object are silently dropped (Claude adapter mirrors this at claude.py:278), so a turn where the model asked for a tool can degrade into a final answer made of its narration text.", "failure_scenario": "Provider returns finish_reason tool_calls with content 'Let me check the lead history first.' and one entry whose id is null -> _read_tool_calls returns empty -> result has text and no tool_calls -> loop takes the text branch, persists KIND_FINAL with the narration as the draft, marks the run done; the requested tool never runs and no error is surfaced (the finish reason is ignored by the loop)."},
  {"file": "project/app/services/agent/loop.py", "line": 200, "summary": "agent_max_tool_calls is only checked before the provider call (via force_final), so a single response carrying multiple parallel tool calls is executed in full and can overshoot the tool budget by up to a whole turn's worth of calls.", "failure_scenario": "Cap 8; after three turns used=7 -> tools offered -> model returns 4 parallel blocks -> all 4 execute (11 total), exceeding the configured budget; with larger per-turn fan-out the overshoot grows unbounded per turn."},
  {"file": "project/app/services/llm/claude.py", "line": 122, "summary": "Each tool_result Message becomes its own single-block user message, so parallel tool calls produce multiple consecutive user messages instead of the single user message with all tool_result blocks that Anthropic's tool-use contract specifies.", "failure_scenario": "Claude requests two tools in one assistant turn -> fold emits two tool_result Messages -> two back-to-back user messages each holding one block -> deviates from the documented single-message shape on every multi-tool turn (the API merges consecutive user turns so no 400, but the docs state splitting results degrades parallel tool calling), and behaves differently from the OpenAI adapter whose role-tool messages are the native shape."},
  {"file": "project/app/services/llm/claude.py", "line": 135, "summary": "The Claude adapter's else-branch silently rewrites any non-assistant/non-tool_result role (e.g. a future system Message) to role user, while the OpenAI-compatible adapter passes the role through verbatim, so the two providers would receive materially different conversations for the same Message list.", "failure_scenario": "A caller adds a system-role Message: OpenAI-compatible providers receive it as a system message (operator authority) while Anthropic receives it as a plain user turn -> provider-dependent behavior for identical input, undermining the provider-neutral contract chat_types.py promises; no error on either path."},
  {"file": "project/app/views.py", "line": 48, "summary": "OutreachRunView accepts resume_run_id whenever AgentLeadRun rows exist, but plan_outreach only honors resume semantics inside the agent-enabled block (outreach.py:2333), so a resume issued while the flag is off silently re-runs every lead single-shot under the same run id instead of resuming or rejecting.", "failure_scenario": "Crash, restart without the flag, post the resume id -> validation passes -> agent_plans stays None -> fresh single-shot provider calls for every unfinished lead (re-billing exactly what resume was built to avoid) and new rows stamped with a trace_run_id whose persisted trace describes a different, agentic provenance."},
  {"file": "project/app/services/llm/stub.py", "line": 183, "summary": "StubClient.agenerate_chat only ever emits exactly one tool call, always for tools[0], always with empty arguments and a fixed id, so stub-driven tests can never exercise multiple parallel tool calls, non-empty/edge-case argument strings, or id handling — masking the real-adapter bugs above.", "failure_scenario": "Tests run the full agent loop through the stub and pass: one well-formed tool call, one result, canned final -> the empty-string-arguments crash, silent tool-call drop, and multi-tool_result message splitting are all structurally unreachable through the stub, so the suite stays green while real provider paths carrying parallel or oddly-serialized tool calls misbehave."}
]
```

## Overlap notes (for triage)

- Tool-budget overshoot within one response: Angles D, A, E (three independent hits).
- Flag-off resume re-bills single-shot under the old run id: Angles D, A, E.
- Terminal-status (failed/exhausted) runs unresumable, lead vanishes: Angles D, A.
- state.py status-constant mirror of the model's constants: simplification + altitude.
- trace.ts duck-typed 404 vs ApiError: simplification + reuse.
- Adapter agenerate_chat duplication: simplification + reuse.
- Approval hash inlined at two sites: reuse + altitude (as part of the transition-mechanism finding).
