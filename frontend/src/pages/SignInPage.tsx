/**
 * Placeholder. The three states (enter / sent / expired) land in
 * `musansht/mus-38-b-signin-page`.
 */
export function SignInPage() {
  return <div className="container narrow">Sign in</div>;
}
