# Development / test dependencies.
#   pip install -r requirements-dev.txt
#
# The test suite runs on Django's built-in test runner with unittest.mock
# (the anthropic/HTTP clients are mocked), so no extra runtime packages are
# required. These are CI tooling: lint, format check, type check, coverage,
# and the Postgres driver used by the CI matrix leg that runs against a
# Postgres service container (local dev still defaults to SQLite).

-r requirements.txt

ruff==0.16.0
mypy==1.19.1
coverage==7.10.7
psycopg2-binary==2.9.12
