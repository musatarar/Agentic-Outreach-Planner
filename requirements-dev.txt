# Development / test dependencies.
#   pip install -r requirements-dev.txt
#
# The test suite runs on Django's built-in test runner with unittest.mock
# (the anthropic/HTTP clients are mocked), so no extra runtime packages are
# required beyond the base requirements (which already include psycopg2-binary
# for the Postgres leg of the CI matrix). These are CI tooling: lint, format
# check, type check, coverage.

-r requirements.txt

ruff==0.16.0
mypy==1.19.1
coverage==7.10.7

# LLM-judge copy quality eval (evals/run_copy_eval.py, MUS-21). Not needed for
# `manage.py test` -- that suite mocks every provider. inspect-ai is the harness
# scaffolding (dataset/scorer/report); all real LLM calls still go through the
# repo's own provider-agnostic layer.
inspect-ai==0.3.249
